<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= player ? 'Edit Player' : 'Add Player' %></title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #1e3c72, #2a5298);
            color: #ffffff;
        }

        .container {
            margin-top: 5%;
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        h2 {
            font-weight: bold;
        }

        label {
            font-weight: bold;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.8);
        }

        .btn-primary {
            margin-top: 20px;
            background-color: #1e3c72;
            border-color: #1e3c72;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="mt-5 text-center"><%= player ? 'Edit Player' : 'Add Player' %></h2>
        <form action="<%= action %>" method="POST" class="mt-3">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= player ? player.name : '' %>" required>
            </div>
            <div class="form-group">
                <label for="team_id">Team ID:</label>
                <select class="form-control" id="team_id" name="team_id" required>
                    <option value="1" <%= player && player.team_id === 1 ? 'selected' : '' %>>1</option>
                    <option value="2" <%= player && player.team_id === 2 ? 'selected' : '' %>>2</option>
                    <option value="3" <%= player && player.team_id === 3 ? 'selected' : '' %>>3</option>
                    <option value="4" <%= player && player.team_id === 4 ? 'selected' : '' %>>4</option>
                    <option value="5" <%= player && player.team_id === 5 ? 'selected' : '' %>>5</option>
                </select>
            </div>
            <div class="form-group">
                <label for="position">Position:</label>
                <select class="form-control" id="position" name="position" required>
                    <option value="PG" <%= player && player.position === 'PG' ? 'selected' : '' %>>PG</option>
                    <option value="SG" <%= player && player.position === 'SG' ? 'selected' : '' %>>SG</option>
                    <option value="SF" <%= player && player.position === 'SF' ? 'selected' : '' %>>SF</option>
                    <option value="PF" <%= player && player.position === 'PF' ? 'selected' : '' %>>PF</option>
                    <option value="C" <%= player && player.position === 'C' ? 'selected' : '' %>>C</option>
                </select>
            </div>
            <div class="form-group">
                <label for="height">Height:</label>
                <input type="number" step="0.01" class="form-control" id="height" name="height" value="<%= player ? player.height : '' %>" required>
            </div>
            <div class="form-group">
                <label for="weight">Weight:</label>
                <input type="number" step="0.1" class="form-control" id="weight" name="weight" value="<%= player ? player.weight : '' %>" required>
            </div>
            <div class="form-group">
                <label for="points">Points:</label>
                <input type="number" step="0.1" class="form-control" id="points" name="points" value="<%= player ? player.points : '' %>" required>
            </div>
            <div class="form-group">
                <label for="steals">Steals:</label>
                <input type="number" step="0.1" class="form-control" id="steals" name="steals" value="<%= player ? player.steals : '' %>" required>
            </div>
            <div class="form-group">
                <label for="assists">Assists:</label>
                <input type="number" step="0.1" class="form-control" id="assists" name="assists" value="<%= player ? player.assists : '' %>" required>
            </div>
            <div class="form-group">
                <label for="rebounds">Rebounds:</label>
                <input type="number" step="0.1" class="form-control" id="rebounds" name="rebounds" value="<%= player ? player.rebounds : '' %>" required>
            </div>
            <button type="submit" class="btn btn-primary btn-block"><%= player ? 'Update' : 'Create' %></button>
        </form>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
